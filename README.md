# dotfiles-workstation

## Bootstrap

Run the following script to bootstrap the home dir on a new system.

```sh
curl -fsSL https://raw.githubusercontent.com/zapling/dotfiles-workstation/main/bootstrap.sh | bash
```
